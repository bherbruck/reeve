# DECISIONS-MADE — judgment calls made during the autonomous build

One line each: what, where, which doc informed it.

- rusqlite gains the `session` feature at workspace level (Cargo.toml) — required by D16 changeset tier (docs/decisions/storage.md).
- crates/repo-store renamed to crates/revision-store, gix removed from workspace — direct execution of D13 (docs/decisions/delivery.md).

## A1 reeve-types
- ApplicationDescription accepts id both top-level (spec examples) and under metadata (reference sandbox artifacts); effective_id() reads either — src/margo/application.rs, informed by ApplicationDescription-001.yaml vs reference nextcloud margo.yaml
- deploymentProfiles[].type, roles[], peripheral/interface/architecture kept as String with named constants, not enums: pinned fixtures contradict the OpenAPI enums (helm.v3, 'standalone cluster' case, 'GPU', x86_64) — workload-management-api-1.0.0.yaml vs device-capabilities.md examples
- Component.properties and Parameter.value are generic serde_yaml_ng::Value: property sets are profile-type-specific and fixtures disagree on scalar types (wait: true vs "true") — wire-exact preservation
- resources.cpu modeled as untagged CpuSpec {One|Many}: OpenAPI says object, every device-capabilities.md example is an array — original shape preserved on re-serialize
- DeploymentStatusManifest.status read as an object (not []status): the attribute table's []status is contradicted by both the example manifest and the OpenAPI schema — noted as spec typo in doc comment
- Overall-state precedence 'failed > removing > installing > pending > removing > installed' has duplicate 'removing'; second occurrence read as 'removed' (only member otherwise absent) — DeploymentState::severity, deployment-status.md
- StateManifest shaped after Margo UnsignedAppStateManifest: manifestVersion + bundle{mediaType,digest,sizeBytes,url} + apps[{appId,deploymentId,secrets_version}]; bundle always serialized (null when empty) per DeploymentBundleRef's MUST-NOT-omit rule — delivery.md D13 + 08-packaging §10.2
- secrets_version spelled snake_case on the wire (exact token used normatively throughout spec/reeve/); other reeve fields camelCase matching Margo convention
- Render-bundle media type const application/vnd.reeve.render-bundle.v1+tar+gzip coined after Margo's application/vnd.margo.bundle.v1+tar+gzip — D13 names no media type
- ServerCapabilities field named serverVersion (01-framework §3.3 says 'extension list plus server version' without pinning a name)
- HealthSample/DiskSample/MemorySample inner field names (usedBytes/freeBytes/totalBytes) reeve-chosen: §7.2 pins semantics not sub-shape; #[serde(flatten)] extra map preserves extensible fields per §7.2 MUST-ignore-unknown
- Journal backfill wire types (JournalBatch/JournalRecord kinds status|health|lifecycle|gap, JournalAck.ackedSeq) defined from 05-health-journal §7.1/§7.3 prose — no example payload exists in spec
- SSE payload field types: durability-lag generation is String (generation keys are rfc3339-shaped per 07-durability §9.2), lagSeconds/lastSeq u64, rollout wave u32, secret-rotation version u64
- custom-otel-helm-app margo.yaml is parse-only in tests: it ships unsubstituted {{HELM_REPOSITORY}} mustache placeholders which YAML parses as complex mapping keys that the YAML emitter cannot re-serialize; full round-trip covered by the other three ApplicationDescription fixtures
- Spec-markdown JSON example blocks extracted at test time as fixtures (deployment-status.md, device-capabilities.md, 01-framework §3.3, 05-health-journal §7.3); ellipsis placeholder keys ("...") stripped before parsing as they are elision notation, not wire fields

## A4 margo-package
- Accepted helm.v3 as a valid deploymentProfiles[].type alongside the linkml pattern's helm|compose, since the pinned reference sandbox ships it (custom-otel-helm-app/margo.yaml; matches reeve-types profile_type consts) — anything else is an Error.
- Two-tier severity: linkml-required violations are Errors that fail Package::load_dir; divergences the pinned reference artifacts legitimately ship (parameter target naming a profile id instead of a component name, cpu architecture x86_64 outside the enum, dangling catalog resource links like license.pdf) are Warnings retained on Package.warnings — WIRE-EXACT rule says real artifacts must not be rejected, spec/margo wins on the rule itself.
- Parameter default values are checked against their linked schema's dataType only (Warning on mismatch, int widens to double); range/regex rules apply to user-provided values at configuration time — enforcing them on defaults would reject the nextcloud reference package (default port 80 vs portRange min 1024).
- Catalog resource links resolve with a lexical containment check (.. escaping the package root and absolute/URL links are never resolved to disk); missing files are Warnings.
- Skipped regex-based linkml checks (author email, url schema regexMatch) rather than adding a regex dependency; id/memory/storage patterns are hand-rolled charset checks.
- OCI refs parse oci://registry/repo[:tag][@algo:hex] with port-aware tag splitting (colon in last path segment only); PackageSource::parse also accepts dir:// per the Milestone-1 agent fetcher convention in CLAUDE.md.
- metadata.catalog + at least one organization with a name enforced as Errors per linkml required flags — all six pinned fixtures satisfy this.

## A2 desired-state
- REEVE_UUID_NAMESPACE defined as UUIDv5(NAMESPACE_DNS, "reeve.dev") = 06c32e1b-5365-5c68-80a2-6cccfa182cf8 (src/render.rs) so independent implementations can re-derive it — D2 asked for a defined namespace constant
- merged `enabled` defaults to true when unset: an app defined by any layer is desired unless explicitly switched off (D11's `enabled: true|false` read as explicit-off switch; tests/render.rs enabled_false_removes_app)
- application.yaml is the vendored package margo.yaml bytes VERBATIM, not re-emitted — wire-exact by construction (D2 + CLAUDE.md WIRE-EXACT rule)
- canonical emitter (D3) applies to ALL rendered YAML including wire-exact deployment.yaml — keys sorted lexically; semantics unchanged, determinism guaranteed
- env-targeted parameters (pointer matching ^env. case-insensitive — both ENV.X and env.x appear in pinned fixtures) trigger per-service `env_file: [env/<service>.env]` injection into every compose service; values ride in deployment.yaml parameters and the agent materializes the env files (spec/reeve/10-secrets.md §12.3 'rendered compose references them via env_file')
- v1 renders compose profiles only; helm/helm.v3 profiles are RenderError::UnsupportedProfileType (CLAUDE.md substrate rules: compose first, helm later/never)
- profile selection: app.yaml `profile:` matches DeploymentProfile.id; when absent, the sole profile or the sole compose-typed profile is used, else AmbiguousProfile error
- compose packageLocation must be package-local; URLs/absolute/escaping paths error (D11 no fetch-at-render); when absent, compose.yml then compose.yaml at package root; exactly one component per compose profile in v1
- strict tree authoring: unknown app.yaml keys, stray paths in an app dir (e.g. typo'd params.yml), and params.yaml names not declared by the ApplicationDescription are errors, never silently ignored (single-writer tree, D11/D14)
- ${REEVE_REGISTRY} substituted from device context in compose.yml, files/** (UTF-8 files only; binary files pass through verbatim), and resolved parameter values — never in the verbatim application.yaml (D3/D8)
- package.name/package.version must be YAML strings (quote numeric-looking versions) since version keys the vendored packages/<name>/<version>/ dir
- manifest.yaml field spelling: camelCase deviceId/generation/registryEndpoint/revisions.{hub optional, local} (u64 revision ids), matching reeve-types' camelCase reeve-extension convention
- uuid dep (v5 feature) pinned in the crate's own Cargo.toml, not workspace root, to avoid cross-agent Cargo.toml conflicts

## A3 revision-store
- Two streams as a CHECK-constrained TEXT column on one revisions table (not parallel tables) — single blob table shared, one monotonic id space, simpler queries; §8.2 only requires the streams be independent chains.
- Revision ids are globally monotonic across both streams (INTEGER PRIMARY KEY AUTOINCREMENT); parent chains are per-stream (parent = stream head at commit time).
- Idempotency compared against the STREAM HEAD only: re-committing content identical to an older (non-head) revision creates a new revision — matches D13's 'undo = new revision with prior content'.
- commit() takes the full tree manifest each time (root manifest model per D13), not a delta; empty manifests are allowed.
- read_at returns Ok(None) for a missing path but Err(UnknownRevision) for a missing revision id — distinguishes 'file absent' from caller bugs.
- blame(path) spans both streams, ascending id, comparing each revision's digest for the path against its own parent's; removal shows as digest=None.
- Plain rowid tables with explicit PKs (no WITHOUT ROWID) so the D16 session-extension change capture at server level can track them.
- sha2 0.10 pinned in the crate's own Cargo.toml rather than workspace.dependencies, per build-rule preference to avoid root Cargo.toml conflicts.
- Kept AUTOINCREMENT so rolled-back/killed transactions can never reuse a revision id (append-only invariant survives crashes).

## B1 manifest poll
- Default poll interval 30s (spec pins no value; 02-channel notes latency = poll interval without the channel)
- HTTP client is reqwest 0.12 pinned in-crate with default-features off + rustls-tls (no openssl); 30s request timeout so a poll can never hang
- ManifestVersion persisted as u64 bit-cast to i64 in SQLite; compared only in Rust (documented in schema) — test covers epoch 0xFFFF past bit 63
- manifestVersion 0 rejected as invalid (Margo range [1, 2^64-1], first MUST be 1) and journaled as security; first-ever manifest otherwise accepted at any valid value
- 200 response missing an ETag header falls back to sha256 digest of the body so conditional GET still works
- Error classification: unreachable (network/missing dir) journaled 'info' severity — expected offline operation; non-200/304 status or unparseable body journaled 'error'; both continue from last known state
- Bundle digest violating sha256:<hex> grammar rejects the manifest before version evaluation (it could never verify after pull)
- Acceptance is atomic: manifest_state upsert + journal row in ONE transaction; persist failure means not accepted (floor unchanged, retried next cycle)
- applied_state table created now with D5 phase CHECK (planned/applying/applied/failed/removing/removed) and record_applied/applied_apps accessors so B3 has its contract; B1 only reads
- dir:// sources never advertise capabilities (no server) => pure Margo behavior
- Capability probe runs once per startup (restart covers 'on version change'); result is informational only, convergence never depends on it (§3.2)
- Used axum (already a workspace dep) as the mock test server instead of adding httpmock

## C1 identity/auth
- Placement per task suggestion: Identity/Role/extractors + device-token machinery in device-api; human auth modes + role policy in reeve-server/src/auth/ (D1 seam shared, Law 2 kept)
- refinery is UNLINKABLE here: refinery-core 0.9.2 caps rusqlite at <=0.39 while the workspace pins rusqlite 0.40 (session feature, D16) and libsqlite3-sys `links=sqlite3` forbids two copies — shipped a minimal embedded runner keeping refinery's refinery_schema_history table shape (drop-in swap later), sha256 checksums with drift detection, one tx per migration; documented in db.rs module docs
- db::migrate() returns bool 'applied anything' so C6 can honor D16's migration-cuts-snapshot-generation law
- revision-store keeps self-initializing its own DDL on the shared single DB file (Law 2); server migrations own only server tables; two writer connections arbitrated by WAL+busy_timeout for now
- Device tokens: 'rvd_' + 64 hex (256-bit CSPRNG), stored as plain hex sha256 — sufficient preimage resistance for high-entropy random tokens; argon2 reserved for human passwords
- Identity::Anonymous carries no privilege in the type; REEVE_AUTH=none elevation to admin happens only in mode-aware AppState::effective_role (password-mode anonymous stays role-less)
- Proxy mode refuses startup unless BOTH REEVE_PROXY_USER_HEADER and REEVE_PROXY_TRUSTED_CIDR are set; missing peer address or untrusted peer => 401 fail-closed; optional REEVE_PROXY_ROLE_HEADER: absent => admin (proxy gates access), unparseable => viewer (least privilege)
- First-boot setup token is in-memory only (sha256 in AppState), logged at WARN, single-use, burned on success; crash-only: a restart while zero users exist mints a fresh one — nothing persisted
- Sessions: cookie 'reeve_session' holds raw 'rvh_' token, DB stores sha256; sliding expiry (REEVE_SESSION_TTL_SECS, default 7d) with 60s write granularity to avoid per-request writes; expired sessions purged at startup, no background reaper
- Hand-rolled ~60-line CIDR matcher (IPv4/IPv6, v4-mapped canonicalization) and manual cookie parse/set — avoided ipnet and axum-extra deps
- Cookies are HttpOnly+SameSite=Lax without Secure attribute (TLS termination is deployment-specific); noted for packaging docs
- reeve-server restructured to lib (src/lib.rs) + thin main.rs so integration tests and C2..C12 compose the same bootstrap/router
- V1 migration creates minimal devices + device_tokens tables (auth needs the FK target); C2 enrollment extends devices via a V2 migration, never recreates
- Defaults: REEVE_LISTEN 0.0.0.0:8420, REEVE_DATA_DIR ./data (DB at <data_dir>/reeve.db), REEVE_AUTH password
- login/setup return 404 outside password mode (surface does not exist); logout/me exist in all modes
- argon2id via argon2-0.5 defaults with 128-bit getrandom salt through SaltString::encode_b64 (avoids password-hash rand_core feature); dummy-hash verification on unknown usernames against timing enumeration
- No root Cargo.toml edits — all new deps version-pinned in the two crates (sha2 0.10, hex 0.4, getrandom 0.3, argon2 0.5; dev: tower 0.5, http-body-util 0.1, tempfile 3)

## B2 bundle pull
- Atomic dir swap implemented as content-addressed dirs + symlink flip: bundles unpack to work/, validate, fsync, rename to data_dir/bundles/<hex> (presence there ALWAYS means complete+verified), then one rename(2) of a pre-made relative symlink data_dir/bundle -> bundles/<hex>; kill -9 leaves either old or new target, never neither (rename over a non-empty dir is not atomic on Linux; symlink rename is)
- Recovery direction is roll-FORWARD: the swap is the commitment point; if kill -9 lands between swap and DB record, startup recovery records the disk digest (journal event bundle-rolled-forward); if the recorded bundle vanished from disk (external interference), the record is cleared (notable journal event bundle-state-cleared)
- bundle.url interpretation: for http(s) sources it is the OCI repository base (absolute URL, or server-relative /v2/<name> joined to the configured server origin; a full .../manifests/<digest> URL is trimmed to its repo base); for dir:// sources it is an OCI layout directory path resolved relative to the manifest source dir (blobs/sha256/<hex> read directly) — oras/skopeo layout output is directly consumable
- bundle.digest names the OCI image manifest (not the layer); the manifest bytes are verified against it, then exactly ONE layer with mediaType application/vnd.reeve.render-bundle.v1+tar+gzip is required and its blob verified against the layer digest; zero or multiple render layers fail closed
- Unpack is fail-closed: only Regular and Directory tar entries accepted (symlinks/devices/hardlinks rejected, not skipped); any path component that is absolute or .. rejects the whole bundle; file bytes fsynced at write, dirs fsynced bottom-up before the publishing rename
- Bundle bytes are buffered in memory during fetch (render bundles are config-scale; digest verification needs the whole payload anyway); HTTP client timeout 120s
- manifest bundle:null (zero apps) is a no-op for B2 — the current bundle link is left in place; removal convergence belongs to B3 per D5
- sizeBytes never enforced (advisory per reeve-types BundleRef doc; digest is the sole integrity check)
- main.rs runs BundleStore::sync on NotModified (304) as well as Accepted, because 304 does not imply the bundle is in place — an accept whose pull failed or crashed must retry; sync short-circuits (no fetch, no journal) when the recorded+linked digest already matches
- Unreachable pull failures journal at info severity (Law 5: offline is expected operation), all other pull failures at error; GC of old bundles/ dirs is best-effort (failure costs disk, never correctness)

## C2 enrollment
- Enroll wire types live in reeve_types::reeve::enroll (additive) so device-api (serves) and reeve-agent (calls) share one shape; field names snake_case exactly as written in D4 step 1, response {device_id, device_token, resumed}
- Route placement: POST /api/reeve/v1/enroll in crates/device-api behind an EnrollmentService trait (Law 2: no SQLite in device-api); join-token MANAGEMENT (POST/GET /api/join-tokens, DELETE /api/join-tokens/{token_hash}) in reeve-server behind human auth with role >= operator enforced in-handler
- Idempotent re-run (same unexpired token + same hostname) returns the SAME device with a FRESH token — returning the same token is impossible since only its hash is stored; all prior device tokens are revoked in the same tx (D1: one live credential per device); the re-run consumes NO additional use (matched via devices.enrolled_with = join token hash)
- Atomicity vs D4's 'one SQLite tx': all server-table writes (token validate + use count, device row, token revoke+issue) are ONE IMMEDIATE tx; the revision-store device-layer commit is sequenced AFTER on the store's own connection to the same DB file (Law 2 forbids reaching into its tables). Crash between the two leaves an enrolled device with an absent layer dir — semantically identical to an empty layer (D3: absence = inherit) and repaired by the idempotent retry
- Initial desired state = empty device layer marker layers/30-device.<device_id>/.keep committed to the LOCAL stream as a whole-tree snapshot carrying the head forward; author 'system:enroll'; idempotent (present at head => no new revision)
- Stale flagging (D4 wiped-box): plain-token enrollment sets stale=1 on every other device with the same hostname; idempotent re-run and re-enroll clear stale=0
- device_id = 'dev-' + 16 lowercase hex (64 bits CSPRNG); PK collision fails the insert loudly rather than merging identities
- Join tokens: 'rvj_' + 64 hex, sha256-hashed at rest, defaults 24h TTL / 1 use; DELETE revokes (sets revoked_at) rather than deleting rows (audit trail); enroll 401 is deliberately indistinguishable across unknown/expired/exhausted/revoked
- Re-enroll token creation validates the target device exists (404 otherwise); binding enforced by FK with ON DELETE CASCADE
- Agent: hostname detected from /proc/sys/kernel/hostname, /etc/hostname, then $HOSTNAME (no new dependency); reqwest 'json' feature added to reeve-agent; enroll subcommand parsed by hand (two required flags do not justify a CLI framework); server URL trailing slash trimmed before persisting

## C3 authoring API (first run)
- Layer writes are one batch endpoint — PUT /api/tree/layers/{layer} with body {message?, files: {relpath: base64}} — declarative whole-layer replace (file absent from body is removed from the layer); matches the IaC 'apply this directory' flow better than per-file puts.
- File content on the wire is standard (padded) base64 — binary-safe for package resources such as icons.
- Ownership seam is an enum on AppState: Ownership::Root (v1, owns every authorable path) | Ownership::Gateway{owned_prefixes} (C10 populates from tier config); checked via Ownership::check_write(stream, tree_path) which refuses Stream::Upstream unconditionally at EVERY tier including root; authoring handlers additionally hardcode Stream::Local, so upstream is unwritable structurally, not by convention.
- Gateway prefix matching: exact match, or prefix ending in '.'/'/' matches by starts_with (open families like layers/30-device.), or continuation at a '/' boundary — layers/20-site.plant-a does NOT match layers/20-site.plant-a2.
- Package vendoring (PUT /api/tree/packages/{name}/{version}) materializes the upload to a temp dir and validates with margo_package::Package::load_dir — the same loader the render path uses — BEFORE committing; invalid package => 422 and no revision; warnings returned in the response.
- revision-store commits are whole-tree snapshots, so commit_subtree carries every head path outside the target prefix forward and delegates idempotency to the store's head comparison; response reports {revision, changed} where changed = head moved.
- Revision history is a parent-chain walk from both stream heads merged newest-first (default limit 100, max 1000) — no new revision-store API needed; O(chain) per request accepted for v1.
- Path grammar enforced at the API: layer dir ^[0-9]{2}-<label> with label 1..=128 of [A-Za-z0-9._-] starting alphanumeric and not ending '.'; package name/version segments 1..=100 of [A-Za-z0-9._+-] starting alphanumeric; relative file paths reject empty/'.'/'..' segments, backslash, and control chars.
- Writes require Role::Operator, reads Role::Viewer, both via the existing join_tokens::require_at_least gate; author recorded in the revision is the authenticated username ('anonymous' under REEVE_AUTH=none).

## C3 authoring API
- Chose batch 'apply this layer content' semantics over per-file puts: PUT /api/tree/layers/{layer} body is the COMPLETE layer content (rel-path -> base64 bytes); files absent from the body are removed — matches the IaC 'apply a directory' front door (D14) and makes idempotence trivial (one commit, store idempotent vs head).
- File bytes cross the wire as standard base64 in JSON — binary-safe for package resources (icons) without multipart complexity.
- Layer grammar enforced as NN-<label>: exactly 2 ASCII digits + dash + 1..=128-char label of [A-Za-z0-9._-], starting alphanumeric, not ending in '.'; taxonomy names are convention not engine knowledge (D11/D12).
- Package segments (name, version) restricted to [A-Za-z0-9._+-] starting alphanumeric, so '.'/'..'/path separators are impossible; all relative file paths reject empty/./..' segments, backslash, and control chars.
- Ownership gate is a standalone enum (Ownership::Root | Gateway{owned_prefixes}) on AppState; check_write refuses Stream::Upstream unconditionally at EVERY tier including root (section 8.2), so upstream immutability is structural, not call-site convention. Gateway prefix matching is boundary-aware (layers/20-site.plant-a does NOT match layers/20-site.plant-a2). C10 populates Gateway from tier config; v1 defaults to Root.
- Package vendoring validates by materializing the upload into a tempdir and running margo_package::Package::load_dir — the exact loader the render path uses, so what vendors is what renders; validation warnings are returned in the response body.
- Revision history endpoint chain-walks both streams from their heads and merges newest-first — parent pointers are the stream (D13 append-only), no extra index needed.
- Write role floor Operator, read role floor Viewer, enforced in-handler via join_tokens::require_at_least; commit author is the authenticated username ('anonymous' under REEVE_AUTH=none per D1).

## B3 compose provider
- Removal argv includes -f <retained>/compose.yml alongside -p <name> down (D5 writes just `-p <name> down` but also mandates using the retained copy; explicit -f is the only way to guarantee that and is what an operator would type)
- deploymentId precedence in status reports: State-Manifest entry deployment_id, then deployment.yaml id, then app name — both sources are the same deterministic UUIDv5 per D2, so this only matters when one is absent
- 4xx on status ingest marks the row sent after journaling at error severity — a server-rejected body must not wedge the store-and-forward queue behind a poison row; 5xx and network errors hold everything for retry
- ps observation failure after a successful `up -d` degrades to installed-with-detail rather than failing the apply: the convergence action completed, observation is best-effort
- compose ps mapping: dead/unhealthy/nonzero-exit => failed; created/restarting/paused/removing/health=starting => installing; zero containers => vacuously installed
- retain_applied failure leaves phase at applying (non-terminal) because the retained copy is part of the apply postcondition removal depends on; next pass re-runs (up -d idempotent)
- clippy fix only code change this pass: scoped a test MutexGuard so it drops before the second send_unsent await (report.rs)

## B4 secrets fetch
- Env delivery mechanism: no symlink or project-dir override needed — B3 already stages bundle apps to mutable data_dir/apps/<name>/ (provider cwd = data_dir, -f apps/<name>/compose.yml) and preserves env/ across restages, so render's relative env_file: [env/<service>.env] resolves to agent-local files while bundles/<hex> stays immutable; sync_env writes there (creating apps/<name>/env/ even before first staging).
- Resolve wire shape defined in reeve-types/src/reeve/secrets.rs (additive shared module, one line added to reeve/mod.rs) so C7 shares it: request {"secrets": [names]}, response {"secrets": {name: {value, version}}}; a name the device may not read or that does not exist is simply ABSENT (no existence oracle, §12.6); ResolvedSecret has a manual Debug redacting value.
- Target matching (fixtures show components name PROFILE components, e.g. nextcloud-stack, not compose services): a components entry matching the deployment-profile component name applies to all services of its compose file (Margo-exact, v1 = one compose component per app); an entry matching a compose service name narrows to that service (reeve's additive per-service scoping per D15); empty components = all services.
- Failure semantics expressed by mutating Desired, keeping core converge secrets-ignorant (compiler-enforced ext boundary): resolve failure with prior materialized state pins entry secrets_version to the last-applied value (bundle-content changes still apply from last materialized env, §12.3-endorsed; new sv never recorded as satisfied), while a never-materialized app is dropped from the pass (never started with wrong/empty config) and retried next cycle.
- Network thrift: resolve is skipped when the app is fully converged including secrets (applied phase + bundle-dir hash + secrets_version match + all env files present), so steady-state polling makes zero resolve calls; rotation or bundle change breaks the check.
- Missing secret names in a 200 response are an error (secrets-missing, app held/deferred), not a partial write; non-scalar values, newline-bearing values, and invalid env var names from pointers are materialization errors (env_file format is line-oriented).
- Plain (non-secret) env-targeted parameters are materialized by the same ext module with no resolver (works for dir:// sources); resolver construction requires an enrolled HTTP(S) agent, else None.
- Journal events: secrets-resolved (info, names@versions audit metadata per §12.6), secrets-env-updated (info, file names only), secrets-resolve-unreachable/-unavailable (notable, Law 5 expected), secrets-resolve-failed/-missing/-env-invalid/-env-write-failed (error); secret values never appear in journal, logs, or Debug output (test-asserted).
- Both ENV. and env. pointer spellings accepted (both appear in pinned fixtures); resolved values are inserted literally and never re-scanned for references; unterminated ${secret: passes through as literal text.

## C4 render pipeline
- Bundle change detection = sha256 over rendered file set EXCLUDING manifest.yaml: provenance (revision ids, generation) moves with every commit even when a device's apps do not, so hashing it would defeat D3 no-change-no-bump; when content is unchanged the previous bundle (with its previous declared-input provenance) stays current. Side benefit: gzip-encoding nondeterminism across library upgrades can never cause a spurious bump.
- Artifact storage: dedicated bundle_blobs table (digest PK, content, created_at), NOT reused revision-store blobs — render artifacts are derived state with their own lifecycle (replaced per render, purged when unreferenced at startup); revision blobs are authored append-only history.
- Repo naming: device render bundles at /v2/reeve/bundles/<device_id>; StateManifest.bundle.url is that server-relative repo base (matches agent B2 repo_base resolution).
- Per-device pull authorization: only the digests the device's CURRENT manifest references (OCI manifest digest, layer digest, shared empty-config digest) are pullable; superseded digests 404 immediately — an agent racing a re-render simply re-polls. Cross-device and unknown-digest requests are 404, never 403 (no existence oracle, §10.7).
- If-None-Match uses RFC 9110 STRONG comparison (W/"…" never matches) per the §10.2/B1 contract wording ('strong If-None-Match'), noting RFC 9110 itself prescribes weak comparison for If-None-Match; our ETags are always strong so this only affects deliberately-weak client tags. Unquoted tags tolerated.
- Zero-apps device: bundle served as JSON null (present, never omitted — Margo DeploymentBundleRef rule), no OCI artifact stored; first manifest is version 1 (epoch 0, counter 1).
- Per-device render errors (authoring mistakes) degrade to keep-last-good: previous manifest stays current, rendered_revision is not advanced so every poll retries, error surfaced in render report + logs; they do not block the pass or the last_rendered_local marker (only a new commit can fix them).
- Device enrolled after the last pass / missed by a crashed pass: GET manifest runs ensure_current (render-on-demand) — no hook into enrollment needed. Device layer-chain edits (no API yet) are picked up by POST /api/render.
- REEVE_REGISTRY env (Config.registry_endpoint, default localhost:<listen port>) is the D8 tier registry endpoint fed to RenderContext — a declared render input, never read from env inside the render path.
- Capabilities advertisement returns an empty extensions list: no server-side wire extension is implemented yet, and we only advertise what is compiled in; the cfg!(feature="ext-<name>") append pattern is documented at the single insertion point (server_capabilities in delivery.rs) for future ext modules.
- Render passes run synchronously in the request path (authoring PUT, manifest GET on-demand, POST /api/render); locks are std::sync and never held across await, lock order fixed as revisions-before-db module-wide.
- generation is a declared render input recorded in manifest.yaml, so byte-identical bundles require identical generation too; determinism is asserted for identical input sets (unit pack test + e2e first-render-twice test), while content_digest keeps generation drift from ever causing a version bump.

## C5 status ingest
- Live-path journal stores the VERBATIM request body (handler passes raw_body alongside the parsed manifest) so unknown extensible fields survive forensically instead of a lossy struct re-serialization (05-health-journal §7.2).
- JournalAck contiguity = end of the run starting at the LOWEST journaled seq (first hole stops the ack); empty journal acks 0; an empty batch is a valid ack query.
- Vanilla Margo reports (no reeve key) are accepted per §3.2: they update last_seen and current state but are NOT journaled (no (deviceId,seq) identity exists); an un-seq'd report never overwrites a seq'd current row, and un-seq'd rows update in arrival order (Margo's own connected assumption).
- Equal seq re-applies to deployment_status_current (>= guard, crash-resend friendly) while the journal row is never overwritten (INSERT OR IGNORE) — 'MUST NOT overwrite' binds the journal, current is derived.
- Wrong-device on both POST surfaces is 403 (Margo's untrusted-client analog), unlike delivery's 404 policy — POST paths name the caller's own id, no existence confidentiality at stake; missing/invalid token stays 401 via device_auth.
- Body deviceId mismatching the caller is 422: child-device (see-thru gateway) reporting is known-unmodeled in v1 (01-framework §3.7 note), so the claim is refused, not silently accepted.
- Manifest poll now touches devices.last_seen_at (delivery.rs): the poll IS the liveness signal pre-C8 — an idle agent 304-ing forever must read online. journal/status ingest also touch it.
- Presence threshold is a module constant (DEFAULT_ONLINE_THRESHOLD_SECS=90, 3x default poll interval), not a Config field — avoids touching the shared Config constructor contract mid-swarm; presence::device_presence returns None for unknown devices (unknown, never guessed).
- Journal seq is stored as i64; a wire seq > i64::MAX is a 422 semantic error (StatusIngestError::Invalid), never a silent truncation.
- Backfilled status-kind records whose payload fails to parse as a DeploymentStatusManifest are journaled as-is but skipped for current-state materialization (journal is forensic, current is derived); parseable ones feed the same max-seq upsert.
- Health classification (§7.4 device- vs link-degraded) and health-state events deliberately deferred to the status-stream task; presence.rs documents the seam and preserves the offline='link down, never device dead' asymmetry.

## B5 channel client
- Channel wire shapes live in reeve-types::reeve::channel (not agent-local) so server-side C6 shares exact frames; ping/pong nonce is a string, nudge scope a plain string (unknown scopes ignored, tolerant reader §3.4)
- Capability gate (rev-001/1, exact version) is probed by the channel task per connection attempt on the backoff schedule, not once at startup — a server that comes up or upgrades after the agent is discovered without an agent restart; any probe failure counts as not-advertised (§4.1)
- Unknown control types absorbed via #[serde(other)] ControlFrame::Unknown; malformed JSON control frames are also ignored (logged debug), never a channel teardown
- Nudge coalescing = capacity-1 mpsc + try_send; rate limiting lives in next_cycle: a nudge inside the 5s window waits out the remainder (draining queued nudges) instead of being dropped, then triggers one cycle — a nudge-triggered cycle IS a full poll, so inter-poll gap never exceeds the interval (§4.4)
- B5 ships server-opened sub-channels fully (parity check: server even, dup-id reject, 16-cap reject, unsupported-purpose reject) plus the odd-id AgentIds allocator; agent-initiated open (accept/reject tracking) deliberately deferred to the first extension needing it (B6+) — until then accept/reject frames are ignored
- tokio-tungstenite pinned to 0.29 = the exact version axum 0.8 ws uses (one copy in the tree), rustls-webpki-roots, optional dep tied to ext-channel; futures-util optional likewise; tokio test-util added as dev-dep for paused-clock timing tests
- Backoff full jitter uses an in-module xorshift64 seeded from wall clock — no rand crate; jitter only needs to decorrelate reconnect storms (§4.5)
- nudge scope "config" is received but ignored (debug log): no agent config-fetch mechanism exists yet; only desired-state triggers a cycle
- Agent applies §4.7 resource limits on its own side too: 1 MiB max message/frame via WebSocketConfig
- Log-once semantics for channel unavailability reset after any connection that lived >= 60s (the same condition that resets backoff)

## B6 terminal
- Enablement mechanism (spec 5.2 leaves it open): a rendered config file at bundle-relative config/terminal.yaml (TERMINAL_CONFIG_PATH in reeve-types), shape = TerminalConfig {enabled, shell, idleTimeoutSecs, hardCapSecs} camelCase YAML; absent bundle, absent file, or unparseable file all evaluate to DISABLED (default-deny) — parse errors are warned about, never trusted as enabled
- Enablement is re-evaluated inside converge_and_report after every converge pass (agent follows its LAST CONVERGED desired state per 5.2, works offline per Law 5); transition to disabled calls gate.kill_all — live sessions terminated with reason 'terminal disabled in desired state'
- PTY identity: the agent's own process identity — the same workload-execution identity the compose provider applies workloads with; enablement config selects the program via `shell` (default /bin/sh); no privilege-switching path in v1 (satisfies 5.3 'never an unconstrained root shell by default' at the same privilege level as workload execution)
- In-band payload encoding (agent-owned per 5.1, published in reeve-types for the UI since the bridge relays opaquely per 5.5): 1 discriminator byte — 0x00 raw terminal bytes, 0x01 resize with u16 BE cols + u16 BE rows; unknown discriminators/runt frames ignored (tolerant reader), never session-fatal
- open.meta wire shape: {sessionId (required), cols (default 80), rows (default 24), term (optional, TERM env default xterm-256color)}; open with missing/malformed meta or sessionId is rejected with a reason, never accepted
- Session limits (5.3 RECOMMENDED, agent side): idle timeout 300s and hard cap 3600s enforced by a 250ms-tick watchdog; both configurable per-device through the enablement config item; close reason names the limit hit
- Child reaping: the PTY reader thread owns the child and calls wait() after EOF — every death path (exit, peer close, disable, idle/cap kill, channel teardown) funnels through kill -> master EOF -> reap, asserted zombie-free in tests via /proc/<pid>/stat
- portable-pty pinned exact (=0.9.0); ext-terminal = ["ext-channel", "dep:portable-pty"] added to default features
- Audit (5.4) is reeve-server's duty; the agent contributes structured tracing only — no session state or content is persisted agent-side (kill -9 leaves nothing resumable, Law 3)

## C6 durability
- Writer unification via RevisionStore::from_shared(Arc<Mutex<Connection>>) locking per call (plus from_connection for owned use) — keeps AppState.db type and every existing call site unchanged; lock discipline documented in state.rs: revisions may briefly take db inside store calls, code holding db must never call into revisions (one-direction, audited, no cycles).
- Changeset tier lives at src/durability/changeset.rs (not src/ext/) — the C6 'one Durability trait in one module' seam requirement wins over the charter's src/ext/ layout; still a whole module behind ext-durability-changeset, core-invisible, --no-default-features proven. Changeset REPLAY stays core so a core binary can restore targets written by a changeset-enabled one.
- No capabilities entry added: REV-006 is marked '— (server-internal)' in spec/reeve/01-framework.md's extension table — it is not a device-wire extension, so /api/reeve/v1/capabilities is unchanged.
- A generation is cut at EVERY process start with a tier enabled (not only after migrations): the changeset session dies with the process (Law 3), so a fresh anchor is the crash-only way to guarantee a gapless chain; snapshot-only tier pays at worst one extra snapshot. Migration-cuts-generation (D16) rides the same path via migrated_at_boot.
- Generation id uses basic ISO-8601 UTC (20260710T211427644Z-<schema>) instead of literal RFC3339 — ':' is unsafe in object keys and Windows paths; ms precision.
- Atomic-or-absent upload relies on object_store semantics (S3 PUT atomic; LocalFileSystem stages+renames) plus gen/latest written last as the only restore entry point — verified by test: unfinalized newer objects and staged '#' files are invisible to restore.
- Recency assertion in verify-restore uses 2x the SNAPSHOT interval against the newest artifact (snapshot or changeset): empty changesets ship nothing (§9.3), so a quiet-but-healthy changeset tier must not read as stale.
- Snapshot flow writes the epoch marker (from settings.server_epoch) at the target if absent, so §9.4's 'marker present and readable' assertion holds from generation one, before any restore ever happened.
- Restore ordering: increment marker at target -> stamp fenced epoch INTO the restored temp DB -> temp+rename into place. Kill -9 anywhere leaves either no DB (restore reruns, double-increment harmless per §9.5) or a complete DB already fenced — the server can never serve under an unincremented epoch.
- Changeset upload failure keeps extracted changesets in an in-memory pending queue (retried in order next tick, degraded flag set); loss of that queue to kill -9 is inside the RPO by definition. If a generation cut's upload fails, shipping pauses (session_generation=None) rather than mis-chaining — the next successful snapshot re-anchors.
- Session 'static transmute + unsafe Send documented: session pointers reference the heap sqlite3*, pinned by an Arc clone of the writer mutex; every create/extract/drop happens under the db mutex. rusqlite 'hooks' feature added (commit counter for the every-M-commits trigger).
- REEVE_DURABILITY=changeset on a --no-default-features binary is a startup refusal with a clear message (config validates tier vs compiled features); tier != none without REEVE_DURABILITY_TARGET also refuses startup.
- verify_restore_runs failure rows double as the §9.4 'never verified => no effective tier' signal, exposed as effective_tier in GET /api/durability/status (viewer+).

## C7 secrets vault
- Vault master key is loaded lazily per operation via keyfile::load_or_create(data_dir/secret.key) instead of adding a field to AppState — avoids breaking concurrent agents constructing AppState literally; 32-byte read per op is negligible.
- device_manifests gained a secrets_digest column (added inside the new V6 migration, not a schema change to an applied migration): content_digest stays the pure D3 file-set detector, secrets_digest detects rotation; content-same+secrets-changed takes a secrets-only path that reuses the previous bundle (digest, generation, provenance) so §12.4's 'bundle digest unchanged' holds — a naive fold into content_digest failed because a material re-render bumps generation in manifest.yaml and thus the tarball digest.
- Rotation propagation is render_all after every secrets write (cheap: non-referencing devices hit the unchanged path); crash gap closed by a render_dirty settings flag written in the SAME transaction as the vault write and consumed by startup reconcile (Law 3). REV-001 poll-now nudge deliberately left to C8's channel — nudge is optimization, never correctness (§12.4).
- secrets_version = sha256 over resolved (name,version) pairs only; an app with references whose names resolve to NOTHING still gets a stable hash (of the empty set), so CREATING a referenced secret later changes the hash and bumps the manifest — and unresolved refs never thrash.
- Server-side ref scan parses rendered apps/<name>/deployment.yaml as ApplicationDeployment and collects ${secret:<name>} from ALL parameter values (strings recursively) — a strict superset of B4's env-targeted extraction with the identical string parser; a superset only widens the rotation-bump set, never misses one.
- GET /api/secrets metadata list gated viewer+ (matches durability status precedent); all writes operator+. Scope grammar: fleet | class.<n> | region.<n> | site.<n> | device.<id> | reeve-internal, validated on PUT; name grammar [A-Za-z0-9._-]{1,200} (fits the ${secret:...} parser unambiguously).
- reeve-internal operational secret names: zot.upstream.username/password, s3.access-key-id/secret-access-key, tier.<name>.token — typed getters zot_upstream_credentials, s3_credentials, tier_token exported for C10/deploy consumers.
- V6 migration is unconditional (table exists in --no-default-features builds, like V5 for the changeset tier): schema stays stable across feature sets so restore/verify of a full binary's target works from a core binary.
- AEAD decrypt failure during resolve is a loud 500 (wrong keyfile/corrupt row must never be silently indistinguishable from 'unknown name'); rate limiting on the resolve endpoint deferred (audit-counting satisfied via metadata-only tracing of device + name@version).

## B8 self-install + self-update
- Agent-update recognition marker: an app dir containing agent-update.yaml (root, then files/ — the tree-authored config path) routes to the A/B updater instead of the compose provider. §10.5 fixes the mechanics, not the marker; the shipped package's component property reeveAgentUpdate names the vendored descriptor and the render pipeline (C4/C12) must place it in the app dir as agent-update.yaml.
- AgentUpdateSpec shape (agent-update.yaml): {version, binary:{url, digest, sizeBytes?}} camelCase; url is absolute http(s), server-relative (/v2 blob route — §10.4 artifact), or a path inside a dir:// source; digest sha256:<hex> is the sole integrity check.
- Restart mechanism is §10.5's "rename-and-exec" reading: after the atomic current-symlink swap the daemon exits 0 and the unit's Restart=always re-execs through the symlink — the unprivileged agent user cannot call systemctl, and crash-only means the exit IS the restart request (phase left at `applying`; the NEW binary's first converge records `applied` once running_version == spec.version, so kill -9 anywhere re-converges).
- A/B layout under /usr/local/lib/reeve-agent (config install_dir): reeve-agent-<version> files + current/previous RELATIVE symlinks + held marker; swap order = retain previous, then flip current (last step per §10.5); re-running a completed swap never re-flips previous (would lose the retained binary); gc removes only unreferenced versioned binaries.
- Health window / rollback = boring systemd option: agent unit Restart=always + StartLimitIntervalSec=300/StartLimitBurst=3 + OnFailure=reeve-agent-rollback.service; the rollback unit ExecStarts `<lib>/previous rollback` THROUGH the retained binary (a broken new binary cannot block its own rollback), guarded by ConditionPathExists=<lib>/previous.
- Anti-flap hold: rollback writes a `held` marker naming the abandoned version; the updater reports Failed for that exact version (what rollout health gates observe, 09-rollouts §11) until desired state names a different one, which clears the hold.
- Binary prefetch happens in the poll loop beside the bundle pull (update::prefetch), never inside converge — converge stays local-state-only (Law 5); offline fetch failures journal and retry each cycle while the app reports Installing.
- install runs the SAME A/B machinery as self-update (install is the first swap of the running binary, from current_exe); systemd unit emission is a standalone module (systemd.rs UnitFile builder) per §10.3's "same machinery as the systemd-unit Provider".
- install writes the config SHAPE (all-commented template, 0600, agent-owned) only when agent.toml is absent; `systemctl start` is skipped when the config doesn't parse (not enrolled — a shape-only config would just flap the unit) but enable always runs; `install --server --token` enrolls first (D4) and then starts.
- Service runs as system user reeve-agent (useradd --system, docker group joined when it exists for the D5 compose provider); unit references the credential FILE via Environment=REEVE_AGENT_CONFIG, never inlines secrets; data dir + lib dir agent-owned so converge and self-update work unprivileged.
- uninstall reverses units/binaries/user but KEEPS agent.toml + data dir unless --purge — an uninstall/reinstall must not silently destroy a device identity.
- libc became an unconditional reeve-agent dependency (was ext-health-only): the core installer's root check needs geteuid; ext-health keeps only httpdate.
- converge kept its 4-arg signature (delegates to new converge_full with Option<&AgentUpdater>) so B3/B4 call sites and tests are untouched; update apps with no updater configured fail loudly.
- Example package profile type is `compose` (the linkml ^(helm|compose)$ pattern margo-package enforces) with the reeveAgentUpdate extension property — additive, never a redefinition; package validates warning-clean via margo-package in reeve-agent's test suite.

## B7 health+journal
- Unified seq space across live + backfill paths: server ingest (reeve-server/src/ingest.rs) stores live reeve.seq and backfill records in ONE status_journal table keyed (deviceId,seq) with a contiguous-run ack — separate counters (old status_reports AUTOINCREMENT vs a new journal counter) would collide on the server and wedge the ack, so record_status now allocates from wire_journal in one transaction and status_reports.seq references it. Pre-B7 DBs seed the counter past MAX(status_reports.seq).
- Lifecycle records (§7.1: start, converge begin/end, provider errors) are mirrored from the existing journal table into wire_journal by a SQLite AFTER INSERT trigger — every existing journal() call site (record_phase, record_bundle, record_accepted, poll events) is covered atomically with zero call-site churn.
- Retention (§7.1): age-based eviction (journal_retention_days, default 30) touches ACKED records only; the size bound (journal_max_bytes, default 512MiB) walks oldest-first and may force out unacked records, appending exactly one gap record {evictedFromSeq, evictedToSeq, records}. AUTOINCREMENT guarantees evicted seqs are never reused.
- Provider grew a restart_counts() trait method defaulting to None (§7.2 names the active Provider as the source); CommandComposeProvider does not implement it yet because `docker compose ps` does not expose restart counts — a per-container `docker inspect` impl can land later without touching the trait, and the sample field is optional by design.
- Health/journal knobs live in agent.toml (health_interval_secs=60 per §7.2, journal_retention_days=30 + journal_max_bytes=512MiB per §7.1) with serde defaults — core config fields, inert without the feature.
- Backfill against a server answering 404/405 on the journal surface disables further attempts until restart (restart re-probes capabilities) instead of knocking every cycle.
- Clock skew (§7.2) is measured from the backfill HTTP response Date header (httpdate, second granularity — drift matters at minutes/days scale) into a shared slot the sampler reads; no dedicated time endpoint.
- Sampler runs on its own SQLite connection (WAL) in a detached tokio task, so it never contends with the converge loop and its failure path is warn+continue only (Law 5 / §3.2).
- Deps pinned exact per charter rule, gated by ext-health: libc =0.2.186 (statvfs; /proc has no disk usage), httpdate =1.0.3 (both already in the workspace lockfile at those versions).
- Sampler is spawned even for dir:// sources and unenrolled agents (journaling is local-first, §7.1); only the JournalSender requires HTTP(S)+device_id, same preconditions as StatusSink.

## B8 self-install/update
- EventHub (src/events.rs) and Channels registry (src/channels.rs) are CORE modules; the feature-gated ext modules populate/serve them. Rationale: producers live in core (ingest, render) and presence/nudge consumers are core; with features off the registry is empty and the hub has no subscribers — compiler-enforced additivity holds (--no-default-features builds and passes), matching the render.rs/ext-secrets precedent.
- Session recording NOT implemented: spec/reeve/03-terminal.md §5.5 is MUST-level 'no recording/replay in rev-002/1' and overrides the build-charter C8 line mentioning optional recording; decision recorded in ext/terminal.rs module docs. If a future rev-002/2 specs it, asciicast-style per-session files behind a config flag would be the seam.
- Terminal UI-leg route is GET /api/reeve/v1/terminal/{device_id} per the task brief (spec §5.1 says {sessionId}); the server mints sessionId at initiation and delivers it via open.meta, audit row, and terminal-session events — one round trip, no separate initiate endpoint. Divergence task-directed and noted.
- rev-004/1 advertised unconditionally: journal/status ingest routes are core (always mounted), and §3.3 says advertise exactly what is usable.
- durability-lag semantics: lag = now - last_changeset_at while pending_changesets > 0 (unknown ship time with pending uploads counts as lagging); events fire only on threshold cross/clear (30s const); verify-restore fires once per newly-finished run; sampler seeds from current status at boot so pre-boot verify runs are not re-announced; sampler not spawned on the none tier.
- Nudges emitted from render_all only (after locks drop, try_send): covers tree PUT hooks, POST /api/render, secrets rotation passes, startup reconcile. ensure_current does not nudge — it runs inside the device's own poll cycle.
- deployment-status events emitted only when the stored overall state actually changed (before/after read around the transactional upsert), so max-seq-rejected and duplicate reports emit nothing; verified in sse_flow test.
- All terminal denial classes audited per §5.4 including authorization failures: none-mode and role refusals write rows attributed as 'anonymous'/username; unknown device 404 also audited. secret DELETE emits secret-rotation with the deleted row's last version (deletion is a version change for referencing apps per §12.4).
- Notify::notify_one (stores a permit) instead of notify_waiters for channel replacement, so a replace landing while the old connection task is mid-frame is never lost.
- Known cross-crate gap (not mine to fix, noted as concern): desired-state render emits only apps/** + manifest.yaml — no layer mechanism yet places config/terminal.yaml into bundles, so terminal enablement cannot currently be authored through the tree; server + agent checks are correct against whatever produces the bundle (tests inject crafted bundles).

## C8 channels+SSE
- Agent-update recognition = agent-update.yaml marker in the app dir (root, then files/); §10.5 fixes mechanics not marker; unusable descriptor FAILS the app rather than falling through to the compose provider.
- Restart mechanism = §10.5 'rename-and-exec': after the atomic current-symlink swap the daemon exits 0 and the unit's Restart=always re-execs through the symlink — unprivileged agent user cannot systemctl; the exit IS the restart request; phase left at `applying` so kill -9 anywhere re-converges and `applied` is recorded only once running_version == target.
- Health window / rollback = boring systemd option: StartLimitIntervalSec=300 + StartLimitBurst=3 + OnFailure= companion unit executing `<lib>/previous rollback` THROUGH the retained binary, ConditionPathExists-guarded; rollback writes a `held` marker so the rolled-back version reports Failed (what rollout gates observe) until desired state names a different version — bounds update/rollback flapping.
- A/B swap order: stage versioned binary -> flip previous to old current -> flip current LAST (each temp+fsync+rename, relative symlink targets); idempotent re-swap never re-flips previous (would lose the retained binary); crash table test asserts current always names a complete binary at every cut.
- Binary prefetch runs in the poll loop beside the bundle pull, never inside converge (Law 5: converge consumes only local state); offline fetch is a journaled retry while the app reports Installing.
- install reuses the SAME A/B machinery (install is the first swap, staging current_exe); start is skipped when agent.toml doesn't parse (not enrolled — shape-only config would flap the unit), enable always runs; `install --server --token` enrolls first per D4.
- uninstall keeps agent.toml + data dir unless --purge (device identity must not be silently destroyed).
- libc promoted from ext-health-optional to unconditional dependency (installer root check needs geteuid); --no-default-features still builds and passes.
- converge kept its 4-arg public signature (delegates to new converge_full taking Option<&AgentUpdater>) so B3/B4/chaos/secrets call sites are untouched.
- Example package uses profile type `compose` (margo-package enforces linkml ^(helm|compose)$) with the additive reeveAgentUpdate component property pointing at the vendored descriptor — extension, never a redefinition; validates warning-clean.

## C9 rollouts
- Per-device target mechanism: a dedicated CORE table device_render_targets (device_id PK, revision, rollout_id, updated_at) rather than a device_manifests column — render.rs honors it unconditionally (V8 migrates regardless of feature), ext-rollouts is the only writer, so a --no-default-features binary keeps a paused rollout's holds stable instead of jumping devices to head.
- Baseline semantics: creation holds un-advanced cohort devices at baselineRevision (request field; default = the rollout revision's parent). Because the authoring commit's render hook has usually already bumped every device to head before the rollout is created, creation RE-PINS cohort devices to the baseline render; devices that polled in the gap converge back. manifestVersion stays strictly monotonic throughout (documented in create_route doc comment).
- Aborted rollouts retain their holds (§11.2 abort = pause permanently; §11.5 never move a manifest backward). A NEW rollout over the same devices is allowed once the old one is aborted/completed and takes over surviving holds verbatim (existing target revision becomes the device's baseline) — this is exactly the rollback-as-new-rollout path.
- Overlap rule: creation is rejected 409 when any cohort device belongs to an active OR paused rollout (fail, not queue — §11.1 implementation choice).
- Pinned/unaffected detection: pure probe (probe_content_digest renders the device at the rollout revision in memory and compares content digests) computed at advancement and stored in the same tx as the target move — crash between probe and tx just recomputes; a device with no stored digest or a failing render counts as affected, never silently green.
- Gate semantics: pass iff converged >= ceil(passFraction x determinable) AND undetermined <= allowance; while not passing and within soak+gateTimeout the gate WAITS (undetermined may resolve via backfill); at timeout it resolves — fail + pause, recording the undetermined set in gate_json. Default undeterminedAllowance = whole wave (Law 5 offline-first default; 0 = strict). Defaults: soak 900s, timeout 900s, passFraction 1.0, failureThreshold 1.
- Health input for gates is deployment_status_current (fresh since advanced_at) — the 05-health-journal §7.4 device/link classifier does not exist server-side yet, so failed deployment reports are the device-degraded proxy and no-fresh-signal is undetermined; backfilled journal records feed the same table under the max-seq rule, giving §7.4-style late reclassification for free.
- tick() performs at most one wave phase change per rollout per call (advancement completes, but a wave advanced in tick N gates no earlier than tick N+1) so status reports always get a window between advancement and gate math — also what makes tests deterministic.
- Abort publishes RolloutPhase::Failed (the rev-003 phase enum has no 'aborted'); gate failure publishes Failed then Paused.
- REV-008 is deliberately NOT advertised in server_capabilities: rollouts are WFM-internal, no agent-facing wire surface (spec §11 intro: agents never know a rollout exists); reeve-types has no rev::ROLLOUTS constant and none was added.

## C12 packaging
- `embedded-agents` defaults OFF (task-level call permitted by spec §10.4 "behind the cargo feature"; the charter's default-ON sentence targets ext-* extensions): the feature is meaningless without cross-built agent binaries handed to build.rs, and a default-ON empty embed would ship an /install that can serve no architecture. Release builds enable it (deploy/ci/build-matrix.yml); the module still builds/tests default-OFF via `--features embedded-agents` with injected dummy artifacts.
- /install routes are wired in run_with_options, NOT router::build, so tests compose ext::install::router(state, artifacts) with dummy binaries without double-registering paths; the without-feature 404 is tested against router::build (which is what a --no-default-features binary serves anyway).
- Agent artifact repos: /v2/reeve/agent/{arch}/{manifests|blobs}/… (arch ∈ x86_64|aarch64), stock OCI image manifest + empty config + one binary layer, `org.opencontainers.image.revision` annotation = GIT_HASH. deploy/packages/reeve-agent agent-update.yaml placeholder aligned to this shape (was /v2/reeve/reeve-agent-x86_64/…; agent's BinaryFetcher treats the url opaquely, so no agent code change).
- /install + agent-artifact auth accepts a VALID-but-NOT-consumed join token (uses stay untouched — enrollment burns it later) OR a device token (§10.5 self-update pulls the same artifacts); REEVE_INSTALL_OPEN=true|1 opens it (Config.install_open, parsed unconditionally, consumed only by the feature). Token may ride ?token= (baked into the script for the curl|sh flow) — it appears in access logs like any capability URL; deployments that care use the Authorization header + out-of-band token entry.
- §10.4 version coherence enforced in build.rs: REEVE_AGENT_BINARIES dir MUST carry a GIT_HASH file matching the workspace revision or the build panics; same-workspace fallback (target/<arch>-musl/release/reeve-agent) is coherent by construction. Missing arch = build warning + script refusing that arch at run time (partial embed tolerated for dev; CI builds both).
- --spec embeds ONLY the numbered spec files (00-INDEX..10-secrets, index order = numeric filename order); the SPEC.md/DECISIONS.md/README.md moved-notice stubs are excluded as non-spec text. specdocs.rs duplicated per binary crate (Law 2: crates stand alone).
- Shell completions hand-rolled (no clap anywhere in the tree — main.rs dispatchers are hand-rolled, so clap_complete would drag a framework in for nothing): --completions bash|zsh|fish generated from the same subcommand/flag tables main.rs matches on; a unit test pins every subcommand into every script.
- `reeve-server init` reads only REEVE_DATA_DIR/REEVE_UPSTREAM from env (not Config::from_env — init legitimately runs before the deployment env is complete, e.g. REEVE_UPSTREAM set but no token yet). Emits the canonical deploy/compose.yml verbatim via include_str! (D9 sync is by construction; packaging_flow test pins it), systemd unit + commented env SHAPE (.env rule), zot-config.json (hub, or onDemand-sync spoke when REEVE_UPSTREAM set) only with --registry.
- `reeve-server healthz` subcommand added (std-TCP probe of /healthz at REEVE_LISTEN): deploy/compose.yml's healthcheck exec's the binary inside a FROM-scratch-ish container where no curl exists; previously the checked-in healthcheck referenced a subcommand that didn't exist.
- openapi.json served unauthenticated at /api/openapi.json (embed-if-present from ui/openapi.json via build.rs; 404 until Track D generates it) — it is shape, not values, and the UI needs it pre-login. SPA fallback (assets.rs) serves embedded ui/dist by path with index.html fallback, but /api/* and /v2/* misses stay real 404s; with no dist embedded every fallback GET 404s (pre-UI behavior preserved).
- Musl: rustup targets + .cargo/config.toml (+crt-static, aarch64 cross linker/CC pins) checked in; x86_64 musl build verified on this machine ONLY if the C toolchain allowed it — see structured-output notes for the honest result (musl-tools was not installable without sudo).

## C10 federation
- Credential source order (recorded): REEVE_ZOT_USERNAME/PASSWORD env pair wins; absent => vault internal-scope zot.upstream.username/.password via the existing ext-secrets typed getter zot_upstream_credentials(), resolved PER REQUEST so vault rotation needs no restart (crash-only: nothing cached); absent both => anonymous toward zot (legal per D8: zot accepts connections only from reeve-server).
- HTTP client (recorded): hyper-util legacy pooled client (client-legacy/http1/tokio features) — the boring choice, same hyper family axum pins; response Incoming body wrapped directly in axum::body::Body for zero-buffer streaming; outgoing bodies are http_body_util::Empty<Bytes> (GET/HEAD carry none).
- http:// only for REEVE_ZOT_URL — any other scheme refuses startup (fail closed). Rationale: the sidecar is co-located and firewalled to reeve-server (D8), so no TLS stack is pulled into the tree.
- Backend 401/403 from zot maps to 502 Bad Gateway, never relayed: zot authenticates reeve-server not devices, so a backend auth failure is proxy misconfiguration; relaying 401 would falsely indict a valid device token.
- Proxy is core-behind-config, not an ext-<name> feature: D7/D8 place the /v2 route split in core delivery; --no-default-features builds include it (vault credential fallback alone is cfg-gated on ext-secrets).
- Pull surface strictly manifests/blobs/tags-list: _catalog, referrers, and blobs/uploads/ paths 404 without touching the backend (device-facing surface only).
- reeve/* namespace 404s in the catch-all as defense-in-depth even though more-specific native routes normally win the match — future native repos (reeve/packages, reeve/agent) can never leak to zot.
- Hop-by-hop headers (RFC 9110 §7.6.1 + keep-alive) stripped both directions; Host, Authorization, Content-Length replaced/dropped on the way to the backend; everything else (Accept, Range, docker-content-digest, etc.) relays verbatim.

## C11 zot proxy
- Verbatim append keeps parent revision ids in a new stream_origins side table (revision_id PK, origin_id UNIQUE, origin_parent) instead of reusing row ids — both streams share one AUTOINCREMENT id space, so parent ids cannot be primary keys; author/message/created_at are stored verbatim on the revisions row.
- Sync revision identity digest = SHA-256 over length-prefixed (origin id, origin parent, author, message, created_at, scope-filtered file manifest); child recomputes on receipt, revision-store enforces the §8.2 divergence-is-an-error rule (VerbatimConflict, surfaced via log + federation-sync SSE, never auto-resolved).
- Scoped secret sync is child-PULL over the same tier credential and loop as revision sync (spec's 'hub syncs DOWN' read as data-flow direction, not dialer); child stores rows re-sealed under its own keyfile with origin='upstream', versions preserved verbatim, prune-on-sync for removed secrets, locally-authored rows never overwritten (warned as §8.4 smell).
- Secret subtree scope at parent = fleet + class.* + region.* + site.<child-site> + device.<id> for devices that token forwarded; reeve-internal never served.
- Air-gap sealed box: X25519 ECIES via x25519-dalek (ephemeral key, key = SHA-256('reeve-sealed-box-v1' || shared || eph_pub || recipient_pub), payload = eph_pub || existing XChaCha20-Poly1305 envelope) — crypto_box-equivalent, pure Rust, no new AEAD dependency.
- Archive format: stock OCI image layout (dir or .tar) with artifactType application/vnd.reeve.federation-export.v1+json; ed25519-dalek 2 signature over the exact index.json bytes in side-file reeve.sig; integrity of the rest follows the content-addressed chain index->manifest->payload->blobs; import verifies EVERYTHING before writing anything.
- Import signer trust: --expect-signer wins, else trust-on-first-use pin in settings.federation_trusted_signer (key distribution is out-of-band per §8.7); tier ed25519/x25519 keys minted at first use as data_dir/tier_ed25519.key / tier_x25519.key via the keyfile temp+fsync+rename discipline; `reeve-server tier-identity` prints pubkeys+fingerprints for commissioning.
- Gateway ownership prefixes derived per §8.4 as layers/20-site.<REEVE_SITE> + layers/30-device. (the D11 numeric-prefix taxonomy convention shared with render.rs layer_chain).
- Root-side delegated-layer refusal is CORE (tree.rs queries tier_tokens + devices.tier_origin; V9 tables exist regardless of features, same rule as V6/V7/V8) — a --no-default-features binary still enforces §8.4 both directions.
- Forwarded devices (tier_origin NOT NULL) appear in status surfaces at the parent but are excluded from render/manifest serving (§8.6: a device converges against exactly one tier).
- Status-forward cursor advances to the last seq SENT on HTTP 200 (the parent persists each batch in one transaction), not to the contiguous ack — pre-existing gap-marked journal holes cannot stall forwarding; (deviceId,seq) idempotency makes resends harmless.
- federation-sync SSE event added additively to the rev-003 table in reeve-types (unknown types ignored by older clients per §6.3).
- Two-stream render change detection: device_manifests.rendered_upstream column + settings.last_rendered_upstream so ensure_current/reconcile re-render when only the upstream stream moved (kill -9 between sync append and render pass heals at startup).
- Tier tokens default to non-expiring (revocation is the lever); default sync scope = layers/00-fleet, layers/05-class., layers/10-region., packages/ — overridable per token; scope enforcement reuses ownership::prefix_matches (one matcher, no drift).
- REEVE_UPSTREAM on a core (no ext-federation) build boots with ownership enforced but logs loudly that sync is not running, rather than refusing startup — local agents keep converging (Law 5 one tier up).

## C12 packaging
- embedded-agents defaults OFF (task delegated the call; spec §10.4 only requires 'behind the cargo feature'): an empty default-ON embed would ship an /install that serves no architecture; release builds enable it (CI example). Recorded in DECISIONS-MADE.md + Cargo.toml.
- /install routes wired in run_with_options, not router::build, so tests inject dummy ArchArtifacts without route double-registration; the without-feature 404 tests router::build directly.
- Agent artifact repo shape fixed as /v2/reeve/agent/{arch}/… (stock OCI image manifest, empty config, one binary layer, org.opencontainers.image.revision=GIT_HASH annotation); deploy/packages/reeve-agent placeholder + update.rs doc comment aligned (agent BinaryFetcher treats url opaquely — no agent code change).
- /install auth: join token validated but NOT consumed (enrollment burns it), device tokens accepted (§10.5 self-update pulls same artifacts), ?token= form baked into script for curl|sh (logged-URL caveat recorded); REEVE_INSTALL_OPEN=true|1 => anonymous (new Config.install_open, parsed unconditionally).
- §10.4 version coherence in build.rs: REEVE_AGENT_BINARIES requires a GIT_HASH file prefix-matching the workspace revision or the build panics (verified); missing arch = warning + runtime refusal for that arch.
- --spec embeds only the numbered files 00-INDEX..10-secrets (index order = numeric filename order); SPEC.md/DECISIONS.md/README.md moved-notice stubs excluded. specdocs.rs duplicated per binary (Law 2).
- Completions hand-rolled (no clap anywhere; adding clap_complete would drag in a framework): generated from the same subcommand/flag tables main.rs dispatches on, unit test pins coverage.
- init reads only REEVE_DATA_DIR/REEVE_UPSTREAM raw env (not Config::from_env — init legitimately runs with incomplete deployment env); emits canonical compose verbatim via include_str! so D9 sync holds by construction, plus a test against the checked-in file.
- Added reeve-server healthz subcommand: the pre-existing deploy/compose.yml healthcheck exec'd a subcommand that did not exist.
- SPA fallback treats /install as API space so a future embedded UI cannot mask the spec-required 404 on non-feature builds.

## D2b UI views + embed
- Added `epoch` to DurabilityStatusResponse (viewer+, additive) and a human `GET /api/server` (viewer+, operationId server_info, tag ops) returning ServerCapabilities: the ops page needs epoch + version/capabilities and the existing /api/reeve/v1/capabilities sits behind device_auth — a browser session cannot call it.
- Tree pages read the "effective head" as upstream-head files overlaid by local-head files (matches D13 two-stream render input); authoring (layer edit, revert, package upload) always targets the local stream, and stream provenance is badged per layer/file.
- Layer editor is a staged working set pre-populated from head content, applied as ONE batch-declarative PUT (D14): delete-in-set = real delete; binary files (strict-UTF-8 decode fails) ride through base64-unchanged and are not editable.
- Revert = per-layer button on revision detail that re-PUTs that revision's layer content as a new local revision (append-only D13; no history rewrite). Blame is a tab in the layer file viewer.
- File content fetching wraps the GENERATED fileAt fetcher/key factory (orval types the body as blob) with a decode-to-text queryFn suffix key `[...getFileAtQueryKey(id,path),'decoded']` — no hand-written API paths/types.
- Added `yaml` npm package (parser only, not a state/routing lib) to render margo.yaml summaries client-side (profiles/components/parameters) and to prefill upload name/version; server-side margo-package validation stays authoritative (422 surfaced verbatim).
- CRUD confirm actions (secret delete, token revoke, rollout abort) use an inline two-step arm/confirm button — the no-modals rule is read as covering confirmation dialogs too.
- Secrets "new" and "rotate" share one form component + one /secrets/set route (same PUT is both); rotate prefills name/scope via search params; the value field is cleared on submit either way and never displayed back.
- Cohort builder ships exactly the task-directed selectors (explicit device checklist, layer-name chips, label k=v chips → CohortSpec union); no richer selector UX invented (NOT-decided fence).
- Tier-token management lives on the ops page (admin-gated card) with a dedicated /ops/tier-tokens/new create page showing the raw token once with copy-to-clipboard, mirroring join tokens.
- Tree commits invalidate the whole Query cache (a head move staleness-touches revisions/files/blame/diff/packages and there is no tree SSE event); narrow key-factory invalidation kept for list-shaped mutations (secrets, tokens, rollouts).
- Styled-native `<select>`/`<textarea>` added as shadcn-style ui components (native-select.tsx, textarea.tsx) instead of pulling radix Select machinery for five plain dropdowns.

## D1 openapi+orval
- reeve-types stays serde-only by default: utoipa is an optional dep behind a new `openapi` feature (cfg_attr ToSchema derives on events/enroll/health/manifest/secrets/capabilities/margo-status types); only reeve-server and device-api enable it — recorded in both Cargo.tomls.
- device-api takes utoipa as a normal dependency (its routes are part of the served contract); it also hosts the shared ErrorBody { error: string } schema used by every JSON error response across crates.
- OpenAPI doc assembly mirrors the CODE BOUNDARY: CoreApi derive + one derive per extension (SecretsApi/RolloutsApi/FederationApi/SseApi/TerminalApi) merged under the same cfg(feature) gates as router.rs, so the printed document always matches the binary's actual surface; ui/openapi.json is generated from the default feature set.
- Handlers that build responses with ad-hoc json! keep their bodies; typed ToSchema structs (CommitResponse, RolloutDetail, FederationStatus, CreatedJoinToken, etc.) document the exact wire shape in the annotations instead of refactoring 15 tested handlers — deeply dynamic corners (rollout gate evaluation, recorded cohort spec) are typed as Object.
- Tier-to-tier sync routes (/api/reeve/v1/sync/*) and the OCI /v2 wire were deliberately left unannotated: neither human- nor device-facing UI surface (task said prioritize human routes; /v2 is the OCI distribution protocol, not JSON API).
- /api/devices journal paging uses a before_seq cursor (seq DESC) rather than offset — stable under concurrent ingest; unknown device is a 404 distinct from an empty journal.
- Presence in device list/detail reuses channels.online_since above last_seen recency directly (avoids re-locking the DB per device inside the list query, matching presence.rs semantics).
- orval config: default react-query split (GET -> useQuery + getXQueryKey factory, writes -> useMutation) — an explicit override.query.useQuery/useMutation flag was tried and reverted because it forces the wrong hook kind onto all operations; namingConvention kebab-case enforces the CLAUDE.md file-name rule on generated files; httpClient fetch avoids an axios dependency.
- `reeve-server openapi` runs before Config::from_env (like --version/--spec) so it needs no runtime configuration; output is serde_json pretty + trailing newline, verified byte-identical across runs (the drift check diffs exact bytes).
- just check-api-drift depends on gen-api then `git diff --exit-code ui/openapi.json ui/src/api` — exactly the D10 CI gate; note it becomes fully meaningful once ui/openapi.json and ui/src/api are committed (currently untracked, so git diff passes trivially until the first commit).

## D2a UI core
- Fixed duplicate operationIds server-side with explicit utoipa operation_id attributes (durability_status, federation_status, rollout_status, list_rollouts, list_secrets) instead of renaming handler fns — no router/test churn; regenerated ui/openapi.json + ui/src/api (hooks: useDurabilityStatus, useFederationStatus, useRolloutStatus, useListRollouts, useListSecrets).
- Build script reordered to 'vite build && tsc -b' because the TanStack Router vite plugin generates src/route-tree.gen.ts at build start; tsc still gates the build (runs after, fails the npm script on type errors).
- Route param file is $device-id.tsx (param key 'device-id') to keep the kebab-case rule; __root.tsx / route-tree.gen.ts are router-mandated names, all lowercase.
- Auth gate lives in /_app pathless layout beforeLoad: ensureQueryData(getMeQueryOptions()); redirect to /login only when effectiveRole is null — REEVE_AUTH=none (anonymous acting admin) skips login exactly as specified. Login 404 (not password mode) navigates straight into the app.
- SSE module (src/lib/sse.ts): one shared EventSource from getEventsRouteUrl(), native browser Last-Event-ID reconnect, reset => invalidateQueries() (everything), reconnect-after-drop also invalidates everything; per-event mapping uses ONLY generated key factories (journal invalidation uses the params-less key as a prefix). Connection state via useSyncExternalStore; usePollInterval(ms) returns ms when SSE down, false when up (30s lists, 10s detail per spec §6.2 recommendation).
- Terminal wire framing taken from crates/reeve-types/src/reeve/terminal.rs: byte 0x00 prefix for data both directions, 0x01 + u16BE cols/rows for resize (UI->agent); connect is click-only, disconnect on unmount, reconnect creates a new session (spec §5.3); ws URL built from getTerminalRouteUrl() with cols/rows/term then scheme-swapped to ws(s).
- Terminal reject reasons: post-upgrade denials surface the bridge's close frame (code 1011 + reason); pre-upgrade HTTP denials (403/409) are opaque to browser WebSocket, so the UI shows a generic denial hint and preemptively disables Connect when the device is offline or the acting role is below operator.
- Journal paging keeps a list of before_seq cursors, one useJournal query per page (generated hook, no infinite-query hand-rolling); only the newest page live-refetches.
- Tree/Packages/Rollouts/Secrets/Enrollment are explicit SectionStub pages (later Track D slices); Ops is functional (durability + federation status cards) since it exercises the SSE durability-lag/verify-restore/federation-sync invalidation paths.
- Dark mode follows the OS preference by toggling the 'dark' class (shadcn convention); no theme-toggle state store added.
- shadcn components installed via CLI (button, badge, card, input, label, separator, table, tabs) with components.json committed for future adds; tsconfig baseUrl omitted (TS 6 deprecation) — paths alias only.

## D2b UI views
- Added `epoch: u16` (required) to DurabilityStatusResponse and read it from settings.server_epoch in the handler — ops page shows §9.5 fencing epoch next to durability posture.
- Added human-facing GET /api/server (viewer+, tag ops, operationId server_info) returning ServerCapabilities, because /api/reeve/v1/capabilities sits behind device_auth and is unreachable from a browser session; registered in router + openapi, regenerated client.
- Tree views read the effective head as upstream-head files overlaid by local-head files (D13 two-stream); authoring (layer edit, revert, package upload) always writes the local stream; stream provenance badged per layer/file.
- Revert = per-layer button on revision detail that re-PUTs that revision's layer content as a NEW local revision (append-only, no history rewrite); implemented by bulk-fetching the revision's layer blobs and re-encoding to the PutFilesRequest base64 shape.
- Layer editor: staged set pre-populated from head; binary files (strict UTF-8 decode failure) ride through base64 untouched and are non-editable; empty staged set is allowed (PUT deletes the layer's files) with an explicit warning label.
- File-content reads wrap the GENERATED fileAt fetcher and key factory with a '[...key, decoded]' suffix queryFn (blob → text|base64) — no hand-written API paths or types; revision content cached with staleTime Infinity (immutable).
- Added npm dep `yaml` (parser only — not a state/routing library) for margo.yaml summaries and upload prefill; server-side margo-package validation remains authoritative and 422s/warnings are surfaced verbatim.
- No-modals rule applied to confirmations too: destructive actions (secret delete, token revoke, rollout abort) use an inline two-step arm/confirm button (auto-disarms after 4s).
- Secrets new + rotate share one SecretForm component and one /secrets/set route (the PUT is both operations); rotate prefills via validated search params; value state cleared on submit success OR failure and never rendered back.
- Cohort builder limited to exactly the task-directed selectors (explicit devices, layer names, labels → CohortSpec union) with waveCount + gate overrides; explicit waves/strategy arrays and richer cohort UX left unbuilt (charter NOT-decided fence).
- Tier-token CRUD lives on /ops (admin-gated card) with a dedicated /ops/tier-tokens/new page; one-time raw token display with copy button mirrors join tokens.
- Tree commits (put_layer/put_package/revert) invalidate the entire Query cache since a head move staleness-touches revisions/files/blame/diff/packages and no tree SSE event exists; list-shaped mutations use narrow generated key-factory invalidation.
- Added shadcn-style native-select.tsx and textarea.tsx ui components instead of pulling radix Select machinery; ConfirmButton and CopyButton added as shared components.
- ops.tsx converted to ops/index.tsx (route path unchanged) to host the tier-tokens child route; section-stub.tsx deleted — no stubs remain.

## shadcn polish
- app-sidebar.tsx: replaced hand-rolled <aside>+<nav> with official shadcn Sidebar (SidebarProvider/Sidebar/SidebarHeader/SidebarContent/SidebarGroup/SidebarMenu/SidebarMenuItem/SidebarMenuButton). NAV items wired to TanStack Router <Link> via asChild; active state via useMatchRoute({to,fuzzy}) -> isActive prop; collapsible='icon'. Layout _app.tsx now wraps <SidebarProvider>/<SidebarInset>; AppHeader got a <SidebarTrigger>. Collapse + mobile (Sheet) behavior comes from the primitive, not custom CSS.
- native-select.tsx: DELETED. Replaced with official shadcn Select (Radix) in all three importers: secret-form.tsx (scope-kind picker), rollouts/new.tsx (revision picker), enrollment/new.tsx (device-binding picker). Radix Select forbids empty-string item values, so: rollouts empty option became a SelectValue placeholder; enrollment's 'None' option uses a '__none__' sentinel mapped back to '' (submit logic unchanged). No genuinely-long-option-list native <select> exception was needed.
- confirm-button.tsx: replaced the two-step inline arm/confirm Button with the official shadcn AlertDialog (Trigger + Content + Cancel + destructive Action). This is a destructive-action confirm (delete/revoke/abort), NOT a CRUD create/edit/detail modal, so it is allowed. Kept the exact ConfirmButton prop API (label/confirmLabel/onConfirm/disabled/size, added optional description) so all 5 call sites (secrets, enrollment, rollouts/$id, tree/revisions/$id, ops) are unchanged.
- Added shadcn primitives via `npx shadcn@latest add`: sidebar, select, alert-dialog, tooltip; sidebar transitively pulled in sheet, skeleton, and the use-mobile hook. Removed the speculatively-added dropdown-menu.tsx because nothing was faking a dropdown (avoid dead code).
- Kept CUSTOM (no shadcn registry equivalent): copy-button.tsx (clipboard-with-ack), app-header StreamIndicator (SSE freshness dot), device-terminal.tsx (xterm.js), and the domain badges deployment-state-badge/presence-badge/rollout-badges (thin wrappers over shadcn Badge). All filenames remain kebab-case; no API types hand-written (generated ui/src/api untouched).
- No package.json changes: all new primitives use the already-installed `radix-ui` unified package + @radix-ui/react-slot, so no new dependencies were introduced.

## Track E verify
- Created crates/e2e as a workspace test-only crate with a feature seam: default feature `ext` turns on all reeve-server + reeve-agent ext-* features; `--no-default-features` compiles a core-only server+agent = the E2 conformance build. Ext-only scenarios gated behind #[cfg(feature="ext")].
- e2e drives the real server + real agent in-process over a real TCP listener; only the workload Provider is substituted (FakeProvider records which apps got up -d / down) because CI has no docker — the exact charter-sanctioned approach.
- Wrote the restore-with-epoch-bump test to match the IMPLEMENTED increment-then-serve semantics (fenced epoch reaches a device on its next re-render; the higher epoch makes an otherwise-lower counter a legal notable bump) — verified this is correct behavior, not a bug. Constructed a genuine anti-rollback scenario: agent floor advanced past the restored counter, then accepts it only because the epoch fenced.
- E1 scenarios already implemented as reeve-server integration flow tests (federation two-server, terminal audit, rollout gate, air-gap round-trip, changeset restore) are cited and mapped in REPORT.md rather than duplicated; crates/e2e adds the genuinely-new cross-crate agent↔server coverage (core loop, agent crash/offline recovery, server startup reconcile→agent pull, epoch-restore, secrets rotation).
- Extended `just conformance` to also run `cargo test -p e2e --no-default-features`; e2e tests: core_loop(4), chaos(3), epoch_restore(1) are core/ungated, rotation(1) is ext-gated, conformance(2) proves capability advertisement reflects compiled features (core build advertises only the unconditional rev-004 health-journal, zero ext-*).
